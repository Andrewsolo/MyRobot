-	не решено
+	решено
?	требует проверки

==========================================================================================================
24.04.2017

?	1. Не останавливается рядом с препятствием
		Проблема в том, что команда "вперед" сразу же делает speed_up, а BD реагирует только через 400 мс!

		В Motor.cpp в Motor::speed_up
		До:		if (Speed == 0) set_speed(MOTOR_SPEED_MIN);
		После:	if (Speed == 0 && MaxSpeed != 0) set_speed(MOTOR_SPEED_MIN);

?	2. Не восстанавливается max speed при "B" сразу после "F" (без "S")
		Добавил barrierdetect_disable() в каждую команду в MA

-	3. Нестабильность поворота servo
		Попробовать зашунтировать пинг, скорее всего дело в нём

==========================================================================================================
25.04.2017

-	1. При команде "F" MA реагирует быстро и увеличивает скорость, а BD медленно определяет, что distance < MIN
